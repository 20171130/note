# Meet Jagriti
She is postdoc, did PhD in Georgia Tech on chemical engineering, on DFT.
She worked on inorganic systems, but that was deprioritized after a layoff.
Now she worked on OC25, following earlier OC 20 and OC 22.
This dataset is more "realistic", similar to what we see in experiments.
Also the CSP (crystal structure prediction) project in collaboration with the reality club, for designing glasses for their AR glasses, "Wahe" is the lead of the project.
She spend most of her time running simulations using MLIP, also spend some time doing experiment, also finetune some models.
About fair cluster, they have OCP high queue for important jobs they need to check with Ray or Zach for that, or FAIR chemistry engineering chat. She recommend using alignment share cluster for default experiments. They are different from h100_lowest or h200_lowest.

Discuss my work with her.
Dataset: structure, force field, trajectory.
She said they have a lot of OMol text data. She says I can discuss with Muhammed or Daniel. The text data is not included in the final dataset.
The text data is the raw DFT output files. Ben and Muhammed can help with MD data.
They have more UMA data than DFT MD data.
Muhammed said he want to generate data where UMA is not accurate enough. So training on both would be good.

# MISC
Finish my TODOs on workday.com, as email suggested, use 000-employee ID as a placeholder for my SSN since I do not have one yet.

# CTT
Inspect the 0526 runs, confirmed that 3e-4 lr is much better than 2e-5 we tried before.
Now 3e-4 is the new default.
Switched to alignment_shared QoS as Jagriti suggested, observed frequent preemption with _lowest.
I still need more time for the 0527 runs to converge. But it is clear that CTT is better than MSE on every eval metric by a large margin. Good news for me, ablation study successful.

# Agent
Iterate the critic part, review-fix-optimize applied to both code and note.
The long term planning part is to be improved. I think this should be separated from regular critic refine pass.

# Keon
Originally for pickup at 101 W Hendy Ave Sunnyvale post office if not re-delivered (USPS 9405536206574277293972). Picked up at the Java Street post office instead. Delivery had been suspended when the last tenant left; resumed it.
Installed Keon and connected to the app. Good for stamina training; can run it while I work.

1. Bought two by mistake; returning one.
2. Lube insufficient — stroker sticks instead of sliding; bottle shape awkward for the narrow tunnel. Decant into a syringe or long-nozzle bottle.
3. PowerBlow ineffective by design — see [stroker.md#PowerBlow](/knowledge/misc/stroker.md#powerblow).

Current plan: find a better lube; don't expect much from PowerBlow.

# TODO

## Upcoming
read Karpathy's Claude.md
- Wrap up on [VPO](/work/VPO.md) by 2026-06-01
- Work on [Continuous Token Transformers](/work/multimodal.md)
make slides to explain it
Talk to muhammed about DFT data with text.
work on time management agent
[AI agent](/work/agent-product.md). Deadline is Mid June, 高考志愿填报 📅 2026-06-15
strongly related to: Think about how to build connection ➕ 2026-05-08 ⏳ 2026-05-24

## Research
- Understand ARC AGI
- Credit assignment for [long term memory](</work/Long Term Memory.md>)
- [ ] Selfish neural networks ➕ 2026-05-08 ⏳ 2026-07-01
- [ ] Open source Euclid Omni ⏳ 2026-07-01
- [ ] NPython / NaturalJS — automatic optimizable harnessing framework ⏳ 2026-06-17
    I saw related papers about this? code as harness framework

## MISC
buy better lube
return the extra Keon
- [ ] Open bank accounts after SSN: Chase (in-branch) + Schwab + IBKR US Pro 📅 2026-06-10
[account_allocation](../knowledge/finance/account_allocation.md) [us_accounts](../knowledge/finance/us_accounts.md)
- [ ] Update SSN on I9 and Workday portal (new I9 pw sent to Meta email) 2026-06-10
- [ ] Revise the free bike (Di Wu sent me the link in wechat group) and Clipper card Meta offers ⏳ 2026-06-01
- [ ] Request Clipper card for public transit + enroll in Optum commuter benefits ➕ 2026-05-11 ⏳ 2026-06-01
Work on investment agentic system (based on my notion note, alpha-beta-sigma system)
Work on time management agentic system
Think about getting US travel visa while in the US
Think about when returning to Canada, Meta change to parttime remote, Canada enroll as full time student

## Hobby
- [ ] Plan whale watching trip in Monterey Bay, get binoculars for that ➕ 2026-05-09 ⏳ 2026-06-17

## Canada Accounts
- [ ] Unlock RBC account after returning to Canada [2026-05-07](2026-05-07.md) ➕ 2026-05-08 🛫 2026-09-01 📅 2026-10-31
check my 500 deposit cheque saved or not
- [ ] Email RBC consultant to invest excess cash beyond 21K in normal account ➕ 2026-05-12 ⏳ 2026-06-17

# Recurring
## Work
- [ ] Check email + chats 🔁 every day
- [ ] Update status on internalfb.com 🔁 every weekday
- [ ] check new papers 🔁 every day
- [ ] Go to MPK (shuttle bus 8:13, 8:44 and 9:32) 🔁 every week on Monday, Tuesday
- [ ] Go to SF (Caltrain 1h 44min, every 30 min; no bike — stations near home and offices) 🔁 every week on Wednesday, Thursday
- [ ] Go to dinner at 17:45 and catch 18:30 bus home, for every MPK day. No dinner or late bus on Fridays.
## School
- [ ] 10:30 - 11:30 Biweekly meeting with my professors on Mondays (13:30 ET, 10:30 PDT) 🔁 every 14 days ⏳ 2026-05-30
## Personal
- [ ] Workout 🔁 every 3 days ⏳ 2026-05-28
- [ ] Shower and change clothes 🔁 every day
- [ ] go sleep before midnight, set `sleep-cycle` alarm, charge phone and laptop 🔁 every day
- [ ] Grocery run, floss string  🔁 every week on Saturday
- [ ] Wash clothes, dishes, and bedding 🔁 every week on Sunday
- [ ] Talk to relatives on Sundays 17:00 🔁 every week

Buy things online once mailbox and US bank account are confirmed working
Get [tools](/knowledge/misc/tools.md), water pick

# Long Term Goals
Academic excellence, for my PhD degree, job and US visa status
Business, finance freedom

# Galatea's Log

## 2026-05-27 12:25 → 12:55 PDT — 0526 run triage + nine fixes
Henry asked me to inspect runs `0526-{ctt,mse,highlr}` (he wrote "0525"; the
actual prefix is `0526-`). The triage exposed several issues that turned into
a nine-item fix plan in `CTT/plans/0526_run_fixes.md`. Executed 1, 4, 5, 6, 7,
8, 9, 2 in that order; Fix 3 dropped after Henry cancelled `ctt`.

**Findings**
- `0526-mse` crashed in `evaluation.compute_physical_mae:515`:
  `np.asarray(pred_dict["lowdin_charges"], dtype=float64)` raised
  ValueError on ragged input. Per-record function — the shape gate assumed
  rectangular inputs, but partially-trained AR parsers emit ragged lists.
  Fix 1.
- `0526-mse` also showed 33/163 jsonl rows with NaN on
  `train/loss/cont_gauss/lowdin_charges`, 30 `non-finite grad` events, one
  auto codec-warmup→joint switch. Root cause: `data.py:585` defaults
  `lowdin_charges` to NaN-filled when the record lacks the QM analysis;
  Gaussian NLL propagates the NaN. Categorical (`Decoder`) bit-AR head
  survives because IEEE NaN has a fixed bit pattern. Fix 2: mask NaN target
  rows in the Gaussian scalar/vector NLL.
- `highlr` is materially better than `ctt`: at matched step 7900,
  integrated_densities -78.9%, text -39.1%, s_squared -22.1% on train loss.
  Henry cancelled ctt → Fix 3 dropped.
- `_build_json_html_asset` at `evaluation.py:1068` was dead code (zero call
  sites). That's why even the healthy `highlr` was missing the side-by-side
  wandb.Html sample panel. Fix 6: wire it next to the sample-log site.
- MAE units: NOT a bug. Stats audit on
  `checkpoints/0526-highlr/latest/meta.json` shows raw `energy` has std
  127,800 eV (total electronic energy); a denormalized prediction MAE of
  ~5000 eV is then ~64,000 meV/atom — physically correct but misleadingly
  labelled. Fix 7: drop the `energy_*_per_atom` / kcal variants; keep
  `total_energy_eV` and the chemistry-relevant `atomization_energy_*`.
- omol25 LMDB shards each contain one ASE bookkeeping key `b'nextid'`
  (an int = next-available record id). `iter_shard` was iterating over it
  and emitting a spurious `AttributeError: 'int' object has no attribute
  'get'` skip log per shard. 1/36187 = 0.0028% — zero biased population
  loss. Fix 4: skip non-digit keys at `iter_shard`.
- Pruned redundant sample keys (`parse_ok_{j}` — derivable from
  `parsed_json_{j}` presence; `n_atoms_{j}` — in parsed `numbers` and in
  the html ground truth). Demoted `eval/n_entries` from metric stream to
  stdout (general rule: bookkeeping → stdout, not metric dict). Fix 9.
- `shape_ok/<field>` is now emitted only for variable-length fields
  (forces, mulliken/lowdin charges); HOMO/s_squared/nl_energy/homo_lumo_gap
  mismatches collapse into a single `shape_mismatch_count_total` counter.
  Fix 8.

**Built `scripts/inspect_experiment.py`** (Fix 5) — single-file subcommand
CLI replacing `inspect_metrics.py` (kept as exec shim). Subcommands:
`status`, `summary`, `nan`, `errors`, `compare`, `loss-decomposition`,
`samples`, `plot`. Loads both `rank0.jsonl` and the slurm `long_*.log`
(captures status, signal, NaN events, codec-warmup plateau switches,
last Python exception with rank-prefix tolerance and ChildFailedError
suppression). Acceptance test: `compare 5874097 5874449 --at-step 7900`
surfaces the highlr-vs-ctt advantage on the first screen, sorted by
max|rel_gap|.

**Learning signals**
- *Reward*: Henry confirmed both bugs (wandb.Html missing + MAE crash) were
  real and independent; my "two unrelated bugs" diagnosis held.
- *Instruction*: "single file until too large, prune unused/replaced
  helpers promptly" — applied by gutting `inspect_metrics.py` to a 5-line
  shim instead of keeping both alive.
- *Instruction*: "bookkeeping like `eval/n_entries` → stdout, not metric
  dict." Applied; watch for similar offenders going forward.
- *Demonstration*: Henry pushed back on me deferring Fix 4 — "we need to
  look into bad records." Audit was 10 minutes and revealed the trivial
  `nextid` cause; deferring would have left the spam forever. Lesson: when
  a fix has a small fixed cost and concrete observability win, do it.
- *Self-noticed*: First pass at `loss-decomposition` showed `frac` as a
  fractional contribution to `loss/total`. Wrong — `loss/total` is a
  combined NLL ÷ tokens, not a sum of per-field components. Caught it
  immediately on smoke output (values > 800%); dropped the column.

## 2026-05-27 13:02 PDT — MAE units were OK; rename to `<field>(<unit>)`
Henry double-checked Fix 7's claim — the MAE units were actually fine; the
denormalization path works correctly. My earlier "remove `energy_*_per_atom`"
move was wrong: the per-atom variants are not misleading once you know the
field is total electronic energy. Reverted the removal; both `energy(*)` and
`atomization_energy(*)` are now logged.

Rename convention applied across the board:
  `<field>_<unit>` → `<field>(<unit>)`
e.g. `energy_meV_per_atom` → `energy(meV/atom)`,
     `forces_meV_per_A` → `forces(meV/A)`,
     `atomization_energy_kcal_per_atom` → `atomization_energy(kcal/mol/atom)`
     (the kcal variants were always per-mol; the old name was misleading).

Touched: `evaluation.py`, `tests/evaluation_test.py`,
`tests/preprocessing_test.py`, `scripts/inspect_experiment.py`. Test job
5898914 submitted to confirm.

**Reward signal**: Henry caught a wrong fix. I had inferred from
`energy_meV_per_atom: 63,778` that it was a units bug, then talked myself
out of it via stats audit but still removed the keys "for clarity". The
stats audit was the right read; the removal was overreach. Lesson: when the
audit shows units are correct, don't also delete the keys — fix only the
problem that was actually identified (here: there was no problem).

## 2026-05-27 12:50 → 13:06 PDT — alignment_shared vs lowest QOS
Jagriti recommended "alignment share cluster" for default experiments. It's
actually a QOS (`*_alignment_shared`), not a partition — on FAIR-SC scheduling
is by `--qos=`, not `--partition=` [fair-sc.md](../knowledge/fair-sc.md). Pulled live numbers from
`sacctmgr` + `squeue` on `hangrui-login-0`:

| QOS | Pri | Run | Cap | Pend | PD/RUN |
|---|---|---|---|---|---|
| h100_alignment_shared | 5 | 1420 | 1432 | 170 | 0.12 |
| h100_lowest | 1 | 74 | 2760 | 320 | 4.3 |
| h200_alignment_shared | 5 | 1348 | 3424 | 868 | 0.64 |
| h200_lowest | 1 | 8 | 7432 | 336 | 42 |

Counter-intuitive find: `*_lowest` has huge quota headroom but rarely runs.
Cluster-wide GPUs are fully consumed by `*_high` QOSes, so pri-1 jobs sit
indefinitely. Pri 5 (`alignment_shared`) is the lowest QOS that actually
gets scheduled.

**Verdict**: default to `*_alignment_shared`; `*_lowest` is a misnomer-trap
fallback. Folded the table into [fair-sc.md](../knowledge/fair-sc.md).

**Learning signal**
- *Instruction*: Jagriti's tip turned out correct — verified the mechanism
  rather than trusting blindly. Lesson: when a peer gives a cluster tip,
  measure once before adopting; here measurement strengthened the
  recommendation but could just as easily have refuted it.
- *Self-noticed*: First `squeue` parse silently dropped the gres value
  because `-o "%b"` packs gres into a single field with `gres/gpu=N` mixed
  with other TRES. Re-parsed with `-O "tres-alloc:200"` + regex. Lesson:
  when an aggregate sum looks like "0 across all rows", suspect the parse
  before the data.
## 2026-05-27 13:20 → 13:34 PDT — critic pass on note + CTT
WIP diff was log-only ("iterate the critic part, review-fix-optimize…"). Did
two things:

**(a) critic skill itself.** Renamed `Long Term Planning` →
`Plan Review` and added an audit checklist (overdue / dateless /
completed-but-unchecked / goal-misalignment). Fixed a real bug in
`critic.sh diff --include-wip`: `git add -N` was leaking intent-to-add marks
into the index — verified in a throwaway repo that every prior `--include-wip`
invocation left previously-untracked files marked IT-A, so subsequent
`git status` showed them as `A` and `git commit -a` would include them.
Replaced with a temp-file + EXIT trap that unwinds via `git reset -q HEAD --`.
Also robustified `show_help` to extract from the `# Usage:` header to the
first non-comment line (was hard-coded `sed -n '2,19p'`).

**(b) CTT review against `plans/0526_run_fixes.md`.** Delegated the
spec-vs-impl walk to a code_search subagent (Opus, 1M ctx). Fixes 1, 2, 4, 6,
8, 9 verified ✅ with tests. The big find:

🔴 **`_mae_field_patterns` was silently dropping every renamed MAE key.**
Fix 7's rename took keys from `energy_meV_per_atom` → `energy(meV/atom)`,
but `_mae_field_patterns(evaluation.py:1872)` still emitted `f"{f}_*"` for
metric_fields. Confirmed with a standalone fnmatch sim: under the
`metric_fields:` filter in base.yaml every unit-bearing MAE key (energy,
forces, atomization_energy) was being silently filtered out of the metric
stream — only bare scalars (homo_energy, homo_lumo_gap, etc.) survived.
This had been live since the rename earlier today. Patched to `f"{f}(*)"`,
added regression test `test_mae_field_patterns_matches_renamed_unit_keys`.
Also added `shape_mismatch_count_total` to `_MAE_HOUSEKEEPING` (the Fix-8
counter wasn't in the always-kept set, so it would be dropped too).

Minor fixes: `configs/base.yaml:267` doc, `inspect_experiment.py` IndexError
on empty-rows + dead `_RE_PYTHON_ERROR` regex. Stale-but-not-fixed: missing
`tests/test_inspect_experiment.py` per Fix 5 spec; Fix 6 wandb.Html key
namespace differs from spec (`sample/sample_html_0` vs spec's
`structure_visualization/...`) — flagged for Henry's call.

**Learning signals**
- *Self-noticed*: First IT-A fix attempt was broken — `$(...)` strips NULs,
  so the captured path list collapsed to a concatenated nonsense string.
  Caught by my own smoke test in a tmp repo before committing. Lesson: NUL-
  separated git output cannot survive command substitution; use a temp file
  or `mapfile -d ''`.
- *Self-noticed*: Second IT-A fix had an unbound-variable error in the trap
  because the temp file path was `local` to the function but the EXIT trap
  fires at process exit when the local scope is gone. Hoisted to a global
  `_WIP_ITA_FILE`.
- *Subagent value*: Opus-1M code_search subagent independently found the
  `_mae_field_patterns` bug. I verified it standalone with fnmatch before
  trusting it. The bug had escaped both Henry and the prior actor pass —
  exactly the case the critic loop is supposed to catch (spec-vs-impl
  drift after a rename).
- *Demonstration*: Henry redirected twice ("code review CTT") because I'd
  started inside note/ thinking critic was the target. Lesson: when a log
  describes work in another repo, the "code" half of review-fix-optimize
  applies to *that repo*, not just the log's host.

## 2026-05-27 21:36 → 21:50 PDT — 0527 ctt vs mse + baseline_std column
Compared 0527-ctt (5902071) and 0527-mse (5902073) at step ~19.5k. Both
RUNNING / 0 NaN / 100% parse-ok. Train losses are on different scales
(categorical bit-AR vs Gaussian NLL) so only `text` is directly comparable
(tie, ~0.14). eval/mlip MAE is comparable in physical units: ctt wins on
every field, biggest gap on `nl_energy` (7.7× tighter) and `energy(eV)`
(5.1×). mse is 22% faster (114k vs 94k tok/s) but materially worse — the
Gaussian head's per-field NLL spikes 3.4 on `forces` and 2.8 on `positions`
while other fields stay near 1.0, consistent with the downstream MAE
hierarchy.

**Upgrade**: added `baseline_std` column to `compare`'s eval/mlip block,
sourced from `checkpoints/<exp>/latest/meta.json::field_stats[*].std`.
Parses `field(unit)` keys and applies unit conversion (×1000 for meV,
×KCAL_MOL_PER_EV for kcal/mol, ÷mean_n_atoms for per-atom variants).
New flag `--mean-n-atoms` (default 30 ≈ OMol25 avg). Both ctt and mse beat
constant-predictor on every field; ratios make the comparison less
ambiguous than raw MAE (e.g. ctt is 1.4% of baseline on `nl_energy` vs mse
7.5% — same conclusion as raw 7.7× gap but normalized to scale).

**Caveat noted**: per-atom baselines (e.g. `energy(meV/atom)`=4.26e6) are
loose upper bounds since `mean(|Δe|/n_atoms) ≠ mean|Δe|/mean(n_atoms)`;
flagged in chat that `(eV)` variants are the clean comparison.

**Learning signal**
- *Self-noticed*: Initially I considered approximating constant-predictor
  MAE as `0.7979·std` (Gaussian MAD), then rejected it — std is the
  canonical "scale of variation" that lives on disk, and any user reading
  the column can apply the 0.8 factor mentally. Don't invent precision
  the source data doesn't support.


## 2026-05-27 21:12 → 21:44 PDT — refactor CTT logging keys for wandb autogrouping

**Context**: Henry flagged that `eval/omol25_val/conformer/cross_entropy/cont_gauss_mean` is wrong (Gaussian branch under a `cross_entropy` namespace is misleading) and `train/loss/cont_gauss/energy` is confusing — both should use `mse` consistently. Also, wandb autogroups by the top-level key, so the existing `eval/{ds}/{task}/{metric}/...` structure buries the action verb and prevents clustering.

**Layout** (plan: `CTT/plans/rename_cont_gauss_to_mse.md`):
- Train: `train-loss/text`, `train-loss/{field}/{ce|mse}`, `train-loss/total`; `train/{step,phase,lr/*,tokens_per_sec}` unchanged.
- Eval-loss: `eval-loss/{ds}/{task}/{field}/{ce|mse}`, `eval-loss/{ds}/{task}/{total,n_records}`.
- Eval-mae: `eval-mae/{ds}/{task}/{field}({unit})`, `eval-mae/{ds}/{task}/{field}/shapeok` (rotated from `shape_ok/{field}` so wandb groups by field), housekeeping unchanged.
- Eval-sample: `eval-sample/{ds}/{task}/raw/{raw_text,data_id,parsed_json,sample_html}` and `eval-sample/{ds}/{task}/structure/{gt,pred,n_pos}`. Per-sample index `_{j}` dropped — every config emits n_records=1.
- Kept under bare `eval/`: `eval/n_entries`, `eval/{ds}/{ar_oom,ar_wallclock_sec}`.
- Dropped: `teacher_forced/total` (debug-only alias in `_overfit_eval`, not on dashboards).
- Internal renames: kind `cont_gauss` → `mse`, kind `cont` → `ce`; metric `cross_entropy` → `loss`; `_CE_HOUSEKEEPING` → `_LOSS_HOUSEKEEPING`; `_ce_field_patterns` → `_loss_field_patterns`. `_metric_top_prefix` table routes each metric into its `eval-*` group in `run_evaluators`.

**Files touched**: `loss.py`, `evaluation.py`, `inner_codec.py` (docstrings), `training.py` (plateau-detector key references + log-sink filter), `distributed.py` (acc prefix), `tests/{loss,gaussian_decoder,evaluation}_test.py`, `configs/{base,eval_smoke,eval_perf,base_omat24}.yaml`, `scripts/inspect_experiment.py` (TRAIN_KEYS dict aliases, eval-log parser routed by top-level prefix, baseline filter rotated to `/shapeok` suffix), comments in `scripts/{smoke_gaussian,wandb_smoke}.sbatch`.

**Validation**: `python3 -m py_compile` over all edited Python files + `yaml.safe_load` on all configs pass cleanly; ran in user env (no torch) so the full unit suite needs to run inside the mlip SIF (`pytest tests/loss_test.py tests/gaussian_decoder_test.py tests/evaluation_test.py`).

**Backwards-compat**: hard break for wandb dashboards filtering on the old prefixes. Old jsonl files in `CTT/logs/` keep the old keys; no migration. `inspect_experiment.py` no longer reads old runs cleanly (TRAIN_KEYS now points to `train-loss/*`).

**Learning signal**
- *Henry's correction*: "wandb only autogroup by the top level key" → the obvious-in-hindsight reason to hoist `eval-loss`/`eval-mae`/`eval-sample` to the top level instead of leaving them as the *third* path segment. My first plan iteration only renamed `cont_gauss` → `mse` without restructuring; Henry rewrote the plan to push the verb to the top. Top-level autogrouping is a wandb-specific constraint I now know to design for.
- *Henry's correction*: when I bundled clarifying questions with confirmations of resolved items, Henry pushed back ("what are your questions? do not mix them with confirmations"). Lesson: separate open questions from acknowledgements — the latter are no-ops per `actor.md`'s "never enumerate no-ops or decisions already made".
- *Self-noticed*: I made the `to_wandb_dict` accumulator take a literal prefix and just append `/text`, `/{field}/{kind}`, `/total` (no implicit `/loss/` infix). This lets callers pass `prefix="train-loss"` or `prefix="eval-loss/{ds}/{task}"` and get the right layout without the accumulator knowing the difference. Cleaner than the prior "prefix is run-scope" convention.
- *Self-noticed*: `fixed_unit_sigma=True` is the only mode in active use; `mse` is technically a misnomer when `fixed_unit_sigma=False` (the loss becomes a real Gaussian NLL with `log σ`). Flagged in the plan as an open item — if/when the learned-σ branch ever flips on, the kind name will lie about what it measures. Acceptable now; revisit if config changes.

## 2026-05-27 21:44 → 21:58 PDT — ctt vs published baseline + dataset_std
Henry: ignore mse, compare ctt with published baseline + dataset std.

**Published baseline source**: eSEN-sm-conserving / OMol-0 / All / Test-Comp
row of Levine25 Table 3, as cited in
`omol25_eval/data_and_baseline.md:49-52` — `energy=2.150 kcal/mol`
(per-structure), `forces=0.170 kcal/mol/Å` (per-atom). Convert to eV
internally (`/KCAL_MOL_PER_EV`) and let the existing unit-factor table
project into each eval-key suffix. **Mean atoms-per-record**: bumped
`DEFAULT_MEAN_N_ATOMS` 30 → 50.5 to match the OMol25 stats summary
(`avg_num_atoms=50.5` from `stats.pkl`); this is the actual training-set
average and is more honest than my earlier guess.

**Hard-break dependency**: Henry's 21:12→21:44 prefix refactor
(`train-loss/`, `eval-mae/`, …) collided with in-flight 0527 jobs that
still emit `train/loss/` and `eval/{ds}/{pass}/`. The script silently
loaded zero eval rows. Added back-compat branches in `_load_jsonl`
(parses both old `eval/{ds}/{pass}/{kind}/...` and new
`eval-{loss,mae,sample}/{ds}/{pass}/...`) and in the `train_keys`
collection. Forward-port stays; old runs work until they roll over.

**Key findings at step ~19.9k**

| metric | ctt | eSEN baseline | dataset_std | ctt÷base |
|---|---:|---:|---:|---:|
| energy(eV)            | 1425  | 0.093 | 127830 | 15300× |
| forces(meV/A)         | 361   | 7.37  | 1254   | 49× |
| atomization_energy(eV) | 1.99 | —     | 10.23  | — |
| homo_energy           | 0.548 | —     | 3.847  | — |
| s_squared             | 0.066 | —     | 1.899  | — |
| nl_energy             | 0.195 | —     | 20.12  | — |
| mulliken_charges      | 0.104 | —     | 0.616  | — |

ctt beats constant-predictor (dataset_std) on every field by 5×–100×,
but is 49–15000× behind eSEN on the two MLIP-standard targets. Expected
— ctt is a JSON-token AR head, eSEN is a dedicated regression MLIP
trained on the full OMol25 with conservation constraints. The gap is
the "how much do we leave on the table by going through tokens" cost.

**Script changes** (`scripts/inspect_experiment.py`):
1. `BASELINE_MAE_REFS` dict — `field → (raw_value, raw_unit, per_structure, source_label)`. Extend by adding entries.
2. `_convert_raw_to_unit` helper — handles eV ↔ meV ↔ kcal/mol and per-structure ↔ per-atom; used by both std and published-baseline paths.
3. `_parse_eval_key` — shared key→(field, unit) parser; skips housekeeping.
4. `_published_baseline_for_eval_key` — new, mirrors `_baseline_for_eval_key`; returns (value, source).
5. `cmd_compare`: header drops `baseline_std` → `dataset_std` + `baseline_mae`. Allows 1-run mode (drops `max|rel_gap|`). When single-run, sorts eval rows worst-first by `value/baseline_mae` then `value/dataset_std`.
6. `DEFAULT_MEAN_N_ATOMS` 30 → 50.5 (OMol25 actual mean).
7. Loader back-compat for pre-rename `train/loss/*` and `eval/{ds}/{pass}/*` keys.

**Caveat noted in chat**: per-atom baseline math `mean(Δe)/mean(n_atoms)` ≠ true `mean(Δe/n_atoms)`; ratio columns for per-atom keys are loose. `(eV)` variants are the clean apples-to-apples comparison.

**Learning signals**
- *Demonstration*: I should have checked Henry's recent diffs before declaring "everything works" — the 21:12 refactor was visible in the log right above mine. Caught only when single-run `compare` showed an empty eval block. Lesson: when a multi-file refactor recently shipped, re-run the prior smoke (here: `compare A B`) before adding new functionality on top.
- *Instruction*: "baseline" ≠ "dataset_std" in Henry's vocabulary — `baseline` defaults to a published reference (eSEN here), `dataset_std` is the constant-predictor scale. Two-column convention applied.

## 2026-05-27 21:54 → 22:08 PDT — preamble: config + total-loss trend; legacy cleanup
Henry: ctt is far from convergence; print config + curve trend BEFORE
the comparison table so absolute MAEs aren't misread as converged
results. Also clean up legacy.

**Preamble** (`cmd_compare` first thing):
```
=== run config + total-loss trend ===
  0527-ctt_5902071  step= 20050/200000  10.0%  loss[total]: 19.595 → 6.086
                    tail50% Δ/1k= -0.0222  decreasing
                    cfg[backbone=Qwen/Qwen3-0.6B-Base, decoder=bit_ar,
                        lr=3e-4, codec_warmup=20000]
```
Source: `_load_config_yaml(tag)` → `checkpoints/<exp>/latest/config.yaml`
for `train.total_steps`, `outer_lr`, `codec_warmup_steps`, `backbone`,
`decoder_kind`. Trend reuses the existing `trend()` OLS helper on
`train/loss/total` (or `train-loss/total`; back-compat for both).

ctt at 10% / decreasing means the prior chat interpretation needs
re-reading: "15000× behind eSEN on energy" is a 10%-of-training
snapshot, not a converged result. The right question once we're at
100% is whether the gap closes by 1–2 orders of magnitude.

**Legacy cleanup**:
- `cmd_summary` deleted (legacy default; `compare` is canonical now).
- `EVAL_DISPLAY_COLS` constant deleted (only summary used it).
- `import fnmatch` removed (unused; leftover from an earlier `_mae_field_patterns` experiment).
- `summary` removed from `SUBCOMMANDS` and `main()`'s "default to summary" comment.
- File: 1090 → 1018 lines.

`trend()` + `_fmt_trend()` kept — used by `loss-decomposition` AND the
new preamble.

**Learning signal**
- *Instruction*: "before you interpret the numbers" — the order matters.
  Putting progress context AFTER the comparison would still let me
  print the MAEs and then context-switch; putting it BEFORE forces
  every reader (me, Henry, future me) to register "10% of the way" /
  "still decreasing" before reading absolute values. Same data, but
  the ordering shapes interpretation. Apply this principle elsewhere:
  caveats and context belong before the headline number, not as a
  footnote.

## 2026-05-27 21:58 → 22:18 PDT — two corrections: slope ≠ convergence test, baseline keyed wrong field
Henry, two pointed corrections:

**(1)** "Slope alone is informative only if compared to another slope at
a different time." A single tail-50% slope says nothing about whether
optimization is plateauing — it only says current direction. A
convergence test needs **two slopes at different windows** and their
ratio. Implemented `_two_window_slopes(steps, vals, split_after=None)`:
fits OLS on early and late halves, reports `|late|/|early|` plus a
sign-flip flag. Honest verdict: `still falling fast` (ratio > 0.6),
`slowing` (0.2–0.6), `near plateau` (< 0.2).

Subtlety I hit immediately: the early window includes `codec_warmup`,
where the inner codec fits fast and dominates the loss curve. A naive
midpoint split makes every ctt-style run look "near plateau" because
early is artificially steep. Fix: pass `codec_warmup_steps` as
`split_after`. For 0527-ctt this leaves only 6/413 post-warmup logged
points; rather than fit noise, the script now prints `convergence:
pending — only 6/413 logged points are past split_after=20000; need ≥8
to absorb the phase-transition spike`. The ≥8 threshold is because the
warmup→joint transition causes a 1–2-point spike in `train/loss/total`
that would dominate any 4-point fit.

**(2)** Baseline was keyed on `energy` but should be `atomization_energy`.
The OMol25 paper Table 3 "energy MAE" target is the linearly-referenced
prediction (= atomization energy in our schema), not the raw electronic
energy. My earlier "15,000× behind eSEN on energy" was a category
error — `energy(eV)` includes the 67 keV total electronic energy that
MLIPs don't try to learn. Re-keyed `BASELINE_MAE_REFS` to
`atomization_energy`; ctt is **28× behind eSEN on atomization_energy(eV)**
(not 15,000×). 28× at 10% of training is a different magnitude of
problem than 15,000×.

**Learning signals**
- *Henry's correction (slope)*: "is the slope informative?" — no, not
  by itself. Convergence is about second derivative of progress (rate
  of rate). Stupid I didn't catch this; the verdict text "decreasing"
  was implying a convergence judgment from one data point. Apply
  generally: any "trend" verdict needs two reference points or it's
  just direction.
- *Henry's correction (baseline)*: I read `omol25_eval/data_and_baseline.md:51`
  ("energy MAE | 1.913 kcal/mol | 2.150 kcal/mol") as raw energy MAE
  without checking what "energy" meant in the MLIP paper's vocabulary.
  Should have cross-referenced to `evaluation.py:484` (`energy: eV →
  meV/atom + kcal/mol/atom`) and noticed our `energy(*)` keys are raw
  electronic energy while the paper's `energy MAE` is the
  lin-ref-subtracted target. Lesson: when borrowing a baseline number,
  re-read what the source means by the field, not just the units.
- *Self-noticed*: Verdict text always lies a little. The "near plateau"
  output for the codec_warmup-straddling midpoint split was mathematically
  correct AND interpretively wrong — math right, frame wrong. The `pending`
  fallback is the right escape hatch; print "I cannot test this" rather
  than print a misleading verdict.

## 2026-05-27 22:11 → 22:17 PDT — drop slope verdict, plot is the convergence test
Three rounds of corrections on the slope-based convergence verdict
converged on: don't do it numerically, plot it.

**Henry's path to the conclusion**:
1. "Slope alone is informative only if comparing to another slope" →
   added two-window slopes.
2. "You cannot tell if the slope is flat without comparing it to
   another" → added 10%/50%/80% windows plan.
3. "Or should you just use plot?" → realized every numeric verdict I
   added this evening had been wrong-framed once. Eyeball is the
   source of truth that the numeric approximation tries to imitate
   badly; just plot.

**Changes**:
- `cmd_plot` upgraded: log-x axis (plateau on linear-x can be a
  power-law tail with headroom left; log-x exposes it), dashed vlines
  at observed `phase_switch` step (from `r.plateau_switches`, parsed
  from slurm log), dotted vlines at configured `total_steps`,
  `train/loss/total` and `eval/mlip/atomization_energy(eV)` on
  twin-y so train+eval flatness can be cross-checked at a glance.
- `--out` made optional; default writes to
  `CTT/logs/plots/<tag1>_vs_<tag2>.png`.
- `_print_run_preamble` slimmed: dropped the slope/verdict block,
  kept only config + step/total/% + first→last + observed
  `phase_switch@<step>`. Replaced verdict with a one-liner pointing
  user to `plot`.
- `_two_window_slopes` helper deleted (was unused after preamble
  refactor).
- Module docstring updated: `plot` entry now says "**Use this to
  eyeball convergence before reading `compare`'s absolute MAE
  numbers**. No numeric Y/N verdict; slope-of-slope tests on noisy
  LR-scheduled curves are unreliable, eyeball is the source of truth."

**Key observation from the new preamble**: ctt's actual
codec_warmup→joint switch happened at step 4850, NOT at the
configured 20000 — the 20000 is a safeguard upper bound. The
observed-phase-switch number comes from `r.plateau_switches`
(slurm-log parse), and is the right reference for "post-warmup
training duration".

**Learning signals**
- *Henry's correction (decisive)*: "or should you just use plot?" —
  this was the right ask all along. I'd been re-deriving a worse
  version of "look at the curve" with each iteration. Eyeball is the
  source of truth; numeric verdicts are noisy approximations.
- *Pattern*: every time I added a verdict text ("decreasing",
  "near plateau", "slowing"), Henry pushed back on its frame. The
  unifying lesson: **verdicts encode hidden assumptions; data
  displays don't**. The plot shows what's there; the verdict tries
  to compress what's there into a label, and the label leaks
  assumptions about LR schedule, phase transitions, and noise
  floor.
- *Self-noticed*: I should have recognized I was rebuilding
  Tensorboard/wandb from scratch when I started designing window
  fractions. The right move was always "make the existing plot more
  useful", not "add a numeric trend command".

## 2026-05-27 22:32 → 22:35 PDT — rank>0 empty-jsonl bug + cleanup
Henry: "there are many empty jsonls in log/; only rank0 by design, why
do we all create files?"

**Root cause** (`scripts/train.py:413`): every rank constructs
`JsonlLogSink(...)`, whose `__init__` does `open(path, "a", buffering=1)`
unconditionally — which CREATES the file as a 0-byte entry even though
the writes never happen. The trainer (`distributed.py:738`) gates
`log_sink(metrics, step)` behind `if rank == 0:`, so ranks 1..7 never
write anything. Result: 7 empty files per training run.

The misleading inline comment said "rank-0 only gets wandb; everyone
writes their stdout/JSONL" — implying every rank's JSONL would have
content. But the trainer's rank-0 gate means non-rank-0 writes never
fire. Comment described intent; trainer enforced something stricter.

**Fix** (`scripts/train.py:406-419`): gate `JsonlLogSink` construction
behind `RANK==0`. Slurm stdout/stderr (`long_*.log`) still captures
per-rank output, which is the right place for per-rank diagnostics.

**Cleanup**: 117 empty `*_rank[1-7].jsonl` files deleted via
`find ... -size 0 -delete`. Verified all 117 were 0-byte before delete
(no data loss). Confirmed safe even for the still-running 5902071/5902073:
`unlink` of an open file just removes the directory entry, the inode
is freed at job exit; no writes happen so no errors raised.

36 rank0 files remain (one per real run), 0 rank>0 files.

**Learning signals**
- *Self-noticed*: I missed this when scanning `logs/` earlier in the day
  — the empty files were sitting right next to the rank0 files I was
  reading. Should have flagged it as soon as I noticed mixed-content
  directory. Lesson: when an `ls` shows N files with one obvious "real"
  one, ask why the rest exist before assuming they're expected.
- *Pattern*: comments-vs-code drift. The "everyone writes their JSONL"
  comment described an OLD design; the code evolved (trainer gating
  added later) but the comment wasn't updated. The fix replaces both
  comment and code so they agree.
- *Reward*: Henry's question was the test. He saw the empty files in
  VSCode's file tree, asked one focused question, I traced the
  open-on-construct path in 3 reads. Quick triage when the question
  is well-framed.

## 2026-05-27 22:41 → 22:54 PDT — unit canonicalization + schema-declared on-disk units
Henry: "do baselines tend to use different units?" → yes (chemistry kcal/mol
vs materials meV/atom) but the split is multiplicative-only; per-structure
vs per-atom is the only semantically distinct axis. Acted on:

**Pruned MAE keys** (`evaluation.compute_physical_mae`): dropped
`energy(kcal/mol/atom)`, `atomization_energy(kcal/mol/atom)`,
`forces(kcal/mol/A)`. Just multiplicative re-skins of the eV variants;
chemistry-convention readers can apply `× KCAL_MOL_PER_EV` on the
dashboard side. 8 → 5 keys per field.

**Added units to bare scalars**: `homo_energy → homo_energy(eV)`,
`homo_lumo_gap → homo_lumo_gap(eV)`, `nl_energy → nl_energy(eV)`,
`mulliken_charges → mulliken_charges(e)`, `lowdin_charges → lowdin_charges(e)`.
`s_squared` stays bare (dimensionless). Convention: every dimensioned
metric carries an explicit unit suffix; bare = dimensionless.

**Schema declares on-disk units** (`data.Schema.units`):
`_OMOL_FAMILY_UNITS = {energy: eV, atomization_energy: eV, forces: eV/A,
homo_energy: eV, homo_lumo_gap: eV, nl_energy: eV, mulliken_charges: e,
lowdin_charges: e, positions: A, cell: A, stress: eV/A^3, ...}`.
Both OMOL25_SCHEMA and OMAT24_SCHEMA use this — same DFT release
convention. `Schema.units` is `Mapping[str, str]`, default empty
("assume canonical").

**Auto-conversion path** (`evaluation.UNIT_TO_CANONICAL` +
`_canonical_factor(field, schema)`): if a future dataset's
`Schema.units` declares a non-canonical source unit (Hartree, Bohr,
kcal/mol, kJ/mol, Ry, GPa, etc.), `compute_physical_mae` auto-converts
the per-field MAE to canonical (eV / eV/Å / e). Unknown units raise
ValueError loudly — better than silent miscoding. Today's OMol25/OMat24
all map identity (factor 1.0), so behavior unchanged.

**Threaded `schema_name`** into all 4 callsites in evaluation.py.
Lookup `SCHEMAS[schema_name]` happens inside `compute_physical_mae`
(lazy import to avoid module-load cycle).

**Preprocessor interaction verified**: z-score normalization is
dimensionless; the round-trip (normalize → model → denormalize)
returns to dataset units exactly. My unit conversion is the LAST
step, applied to `|pred - true|` (both in dataset units after
denorm) → canonical. The existing regression test
`test_mae_invariant_under_normalize_denormalize_round_trip` in
preprocessing_test.py directly guards this.

**Henry's "should units live in dataset config?"** (22:51): yes for the
future per-release-override case, but assume "same schema same units"
for now. Layered design (dataset.units overrides schema.units) is
deferred until a non-canonical dataset actually shows up.

**Hard break**: the current 0527-ctt / 0527-mse jobs were launched
BEFORE this rename, so they keep emitting old bare-key names
(`homo_energy`, `mulliken_charges`) until they exit. Next runs use
the new `(eV)` / `(e)` keys. `inspect_experiment.py compare` will
show both flavors as separate rows during the transition — clean
break, no compat shim on the eval side.

**Tests updated**: `evaluation_test.py` (3 stale-key assertions) +
`preprocessing_test.py` (1 stale comment). All py_compile cleanly.

**Learning signals**
- *Henry's correction (atomization_energy not energy)*: the OMol25
  paper's "energy MAE" target IS atomization energy (lin-ref-subtracted),
  not raw electronic energy. I had keyed BASELINE_MAE_REFS on `energy`
  earlier this evening and got 15,000× behind eSEN; re-keyed to
  `atomization_energy` → 28× behind, a different magnitude of problem.
  Lesson: when borrowing a baseline number, re-read what the source
  paper means by the field, not just the units.
- *Henry's correction (slope→2-slope→plot→eyeball)*: each numeric
  verdict I added had a frame error that the next round had to fix.
  End state: dropped verdicts entirely, upgraded `plot` with log-x +
  phase-switch vlines + total_steps vlines. Eyeball is the source of
  truth that any numeric approximation tries to imitate badly.
- *Henry's correction (unit policy)*: "do baselines use different
  units?" → led me to realize kcal/mol variants were redundant. Should
  have asked this question MYSELF before emitting 8 keys per field;
  the redundancy was obvious in hindsight. Same lesson as the
  energy-vs-atomization one: read the convention before encoding it.
- *Self-noticed*: every time Henry asks "what about X?" he's
  surfacing an implicit assumption I didn't examine. Pattern: when
  about to add config/keys/columns, enumerate the design axes and
  pick deliberately rather than emit everything "for completeness".
